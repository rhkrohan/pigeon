import Foundation

enum MeshConstants {
    static let serviceType = "capsule-mesh"  // Max 15 chars, lowercase + hyphens only
    static let maxHops = 10
    static let maxPeers = 8
    static let messageQueueSize = 500
}
